import Reveal from "../components/Reveal";

export default function Philosophy() {
  return (
    <section className="philosophy">
      <Reveal>
        <blockquote>
          <span>We don&apos;t rush the relationship.</span>
          <span>We build until it feels like you.</span>
        </blockquote>
      </Reveal>
    </section>
  );
}
